import {create} from "zustand";
export type GraphMode="select"|"pan"|"connect"|"new";
export const useWorkspaceStore=create<{mode:GraphMode;selectedEntityId:string|null;selectedEntityIds:string[];selectedConnectionId:string|null;setMode:(mode:GraphMode)=>void;selectEntity:(id:string)=>void;selectEntities:(ids:string[])=>void;selectConnection:(id:string)=>void;clearSelection:()=>void}>(set=>({mode:"select",selectedEntityId:null,selectedEntityIds:[],selectedConnectionId:null,setMode:mode=>set({mode}),selectEntity:id=>set({selectedEntityId:id,selectedEntityIds:[id],selectedConnectionId:null}),selectEntities:ids=>set({selectedEntityId:ids[0]??null,selectedEntityIds:ids,selectedConnectionId:null}),selectConnection:id=>set({selectedEntityId:null,selectedEntityIds:[],selectedConnectionId:id}),clearSelection:()=>set({selectedEntityId:null,selectedEntityIds:[],selectedConnectionId:null})}));