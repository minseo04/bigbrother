"use client";
import {useEffect,useState,type CSSProperties} from "react";
import {ArrowUpRight,Check,ExternalLink,Network,Plus,Rss,Users} from "lucide-react";
import {FeedField} from "@/components/feed-field";
import {NotesPanel} from "@/components/notes-panel";
import type {Article,Briefing,Connection,Entity} from "@/lib/intelligence";
type Props={entities:Entity[];connections:Connection[];briefing:Briefing|null;selected:Entity|null;edge:Connection|null;loaded:boolean;onFollow:(entity:Entity)=>void;onEdge:(connection:Connection)=>void;onEntity:(entity:Entity)=>void;onFeed:(entity:Entity,feedUrl:string)=>Promise<void>};
type ArticleResult=Article&{summary?:string;firstSeen?:string};
function Badge({entity}:{entity:Entity}){return <span className="entity-avatar" style={{"--entity-color":entity.color} as CSSProperties}>{entity.initials}</span>}
export function Inspector({entities,connections,briefing,selected,edge,loaded,onFollow,onEdge,onEntity,onFeed}:Props){const[items,setItems]=useState<ArticleResult[]>([]),[page,setPage]=useState(0);useEffect(()=>{if(!selected)return;const controller=new AbortController();void fetch("/api/workspace?articles="+encodeURIComponent(selected.id)+"&limit=100",{signal:controller.signal}).then(async response=>{const data=await response.json() as {articles?:ArticleResult[]};if(response.ok)setItems(data.articles??[])}).catch(()=>{});return()=>controller.abort()},[selected]);if(edge){const from=entities.find(entity=>entity.id===edge.from),to=entities.find(entity=>entity.id===edge.to);return <aside className="inspector-content"><header><span className="eyebrow">CONNECTION</span><h2>{from?.name} → {to?.name}</h2><p>{edge.label}</p></header><section><h3>Evidence</h3><span className={"evidence-status "+(edge.status==="Hypothesis"?"hypothesis":"")}>{edge.status}</span><p>{edge.evidence}</p><p className="muted">Source date: {edge.date||"Not specified"}</p><a className="source-link" href={edge.url} target="_blank" rel="noreferrer">Read source <ExternalLink size={14}/></a></section><NotesPanel kind="connection" id={edge.id}/><section><h3>Endpoints</h3>{[from,to].filter(Boolean).map(entity=><button key={entity!.id} className="inspector-endpoint" onClick={()=>onEntity(entity!)}><Badge entity={entity!}/><span>{entity!.name}<small>{entity!.kind}</small></span></button>)}</section></aside>}if(!selected){const followed=entities.filter(entity=>entity.followed);return <aside className="inspector-content inspector-empty"><header><span className="eyebrow">WORKSPACE</span><h2>Your intelligence desk</h2><p>Select a node or connection to inspect it.</p></header><div className="inspector-totals"><span><Users size={16}/><strong>{followed.length}</strong> followed</span><span><Network size={16}/><strong>{connections.length}</strong> connections</span><span><Rss size={16}/><strong>{briefing?.articles.length??0}</strong> articles</span></div></aside>}const related=connections.filter(connection=>connection.from===selected.id||connection.to===selected.id),pageSize=8,pages=Math.max(1,Math.ceil(items.length/pageSize)),shown=items.slice(Math.min(page,pages-1)*pageSize,Math.min(page,pages-1)*pageSize+pageSize);return <aside className="inspector-content"><header><Badge entity={selected}/><span className="eyebrow">{selected.kind}</span><h2>{selected.name}</h2><p>{selected.description}</p><button className={selected.followed?"following-button":"follow-button"} disabled={!loaded} onClick={()=>onFollow(selected)}>{selected.followed?<Check size={14}/>:<Plus size={14}/>} {selected.followed?"Following":"Follow"}</button>{selected.source&&<a className="source-link" href={selected.source} target="_blank" rel="noreferrer">Entity source <ExternalLink size={14}/></a>}</header><section><h3>Information</h3>{shown.map(article=><a className="inspector-article" key={article.id} href={article.url} target="_blank" rel="noreferrer"><span>{article.source} · {new Date(article.published).toLocaleDateString()}</span><strong>{article.title}</strong>{article.summary&&<p>{article.summary}</p>}</a>)}{!shown.length&&<p className="muted">No saved articles mention this entity yet.</p>}{pages>1&&<div className="inspector-pages"><button disabled={page===0} onClick={()=>setPage(value=>Math.max(0,value-1))}>Previous</button><span>{Math.min(page,pages-1)+1} / {pages}</span><button disabled={page>=pages-1} onClick={()=>setPage(value=>Math.min(pages-1,value+1))}>Next</button></div>}</section><NotesPanel kind="entity" id={selected.id}/><section><h3>Connections</h3>{related.map(connection=>{const other=entities.find(entity=>entity.id===(connection.from===selected.id?connection.to:connection.from));return <button className="inspector-connection" key={connection.id} onClick={()=>onEdge(connection)}><span>{other?.name}</span><small>{connection.label} · {connection.status}</small><ArrowUpRight size={14}/></button>})}{!related.length&&<p className="muted">No sourced connections recorded yet.</p>}</section><FeedField key={selected.feedUrl??""} entity={selected} onSave={onFeed}/></aside>}